# Name: bgs
# Description: background setter
# Depends on: imlib2 libexif

name=bgs
version=git
release=1
source=(https://github.com/sfplayer247/$name/archive/master.zip)

build() {
	cd $name-master
	make
	make PREFIX=/usr DESTDIR=$PKG install
}
