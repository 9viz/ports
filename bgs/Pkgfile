# Name: bgs
# Description: background setter
# Dependencies: Imlib2 libexif

name=bgs
version=master
release=1
source=(https://github.com/sfplayer247/$name/archive/$version.zip)

build() {
	cd $name-$version
	make
	make PREFIX=/usr DESTDIR=$PKG install
}
