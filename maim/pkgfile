name=maim
version=5.5.2
release=1

source=(https://github.com/naelstrof/$name/archive/v$version.tar.gz)

build() {
    cd $name-$version

    cmake -DCMAKE_INSTALL_PREFIX='/usr/local' \
    SLOP_INCLUDE_DIR=$HOME/usr/local/include ./

    make
    make DESTDIR=$PKG install
}
