# Name: pick
# Description: a fuzzy finder in C

name=pick
version=master
release=2
source=(https://github.com/mptre/$name/archive/$version.zip)

build() {
	cd $name-$version
	PREFIX=/usr MANDIR=/usr/share/man ./configure
	make
	make install DESTDIR=$PKG
}
