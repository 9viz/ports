# Name: uthash
# Description: C macros for hash tables and more
# Depends on:

name=uthash
version=2.1.0
release=1
source=(https://github.com/troydhanson/$name/archive/v$version.tar.gz)

build() {
	cd $name-$version
	mkdir -p $PKG/usr/include
	install -m 0644 src/*.h $PKG/usr/include
}
