name=slop
version=7.4
release=2

source=(https://github.com/naelstrof/$name/archive/v$version.tar.gz)

build() {
    cd $name-$version
    cmake -DCMAKE_INSTALL_PREFIX='/usr/local' -DSLOP_OPENGL="False" ./
    make
    make DESTDIR=$PKG install
}
