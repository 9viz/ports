# Name: zathura
# Description: a documenter viewer
# Depends on: gtk girara intltool

name=zathura
version=0.2.4
release=1
source=(https://github.com/pwmt/$name/archive/$version.tar.gz)

build() {
	cd $name-$version
	make
	make DESTDIR=$PKG install
	rm -r $PKG/usr/share
}
